class Piece {
  /**
   * Creates a new piece.
   * @param {string} symbol - The symbol to represent the piece (e.g., X or O).
   */
  constructor(symbol) {
    this.symbol = symbol;
  }
}

export default Piece;
